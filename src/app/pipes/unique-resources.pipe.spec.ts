import { UniqueResourcesPipe } from './unique-resources.pipe';

describe('UniqueResourcesPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueResourcesPipe();
    expect(pipe).toBeTruthy();
  });
});
